- name: Reboot
  shell: sleep 2 && reboot
  async: 1
  poll: 0

- name: Wait for reboot
  local_action:
    module: wait_for host={{ ansible_hostname }} port=22 state=started delay=30 timeout=60
  become: false

- name: Restart rsyslog
  service:
    name: rsyslog
    state: restarted